<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/data/jpa
       http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd">

    <bean id="dataSourceKit" class="org.apache.tomcat.jdbc.pool.DataSource" destroy-method="close">
        <property name="driverClassName" value="${spring.datasource.kit.driverClassName}"/>
        <property name="url" value="${spring.datasource.kit.url}"/>
        <property name="username" value="${spring.datasource.kit.username}"/>
        <property name="password" value="${spring.datasource.kit.password}"/>
        <property name="initialSize" value="${spring.datasource.kit.initialSize}"/>
        <property name="maxActive" value="${spring.datasource.kit.maxActive}"/>
        <property name="minIdle" value="${spring.datasource.kit.minIdle}"/>
    </bean>

    <bean id="sessionFactoryKit"
          class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSourceKit"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${spring.datasource.kit.hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.hbm2ddl.auto">update</prop>
                <prop key="hibernate.autoReconnect">true</prop>
            </props>
        </property>
        <property name="packagesToScan" value="com.magic.acme.assist.jpa.kit.entity"/>
    </bean>


    <!--hibernate4必须配置为开启事务 否则 getCurrentSession()获取不到-->
    <bean id="transactionManagerKit" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactoryKit"></property>
    </bean>

    <tx:advice id="txAdvice" transaction-manager="transactionManagerKit">
        <tx:attributes>
            <tx:method name="find*" propagation="REQUIRED"/>
            <tx:method name="*" read-only="true"/>
        </tx:attributes>
    </tx:advice>

    <aop:config proxy-target-class="true">
        <!-- <aop:advisor advice-ref="txAdvice" pointcut="execution(* dao.*.*(..))"/> -->
        <aop:pointcut expression="execution(* dao.*.*(..))" id="pointcut"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="pointcut"/>
    </aop:config>

    <jpa:repositories base-package="com.magic.acme.assist.jpa.kit.repository" transaction-manager-ref="transactionManagerKit"></jpa:repositories>


</beans>